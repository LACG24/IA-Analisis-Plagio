def custom_format(num_val):
    """
    Convert a number to a custom format with two decimal places.

    Args:
        num_val (float or int): The number to be converted.

    Returns:
        str: The number in custom format with two decimal places as a string.
    """
    return f"{num_val:.2f}"